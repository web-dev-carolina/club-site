export class Announcement {
    constructor(title, body, date){
        this.title = title;
        this.body = body;
        this.date = date;
    }
    title: string;
    body: string;
    date: string;
}
